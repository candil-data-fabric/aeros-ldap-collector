apiVersion: v1
kind: Service
metadata:
  name: {{ include "aeros-ldap-collector.fullname" . }}
  labels:
    {{- include "aeros-ldap-collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aeros-ldap-collector.selectorLabels" . | nindent 4 }}
